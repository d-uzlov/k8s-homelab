{
  "signing": {
    "default": {
      "expiry": "87600h"
    },
    "profiles": {
      "apiserver": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "87600h"
      },
      "scheduler": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "87600h"
      },
      "controller-manager": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "87600h"
      },
      "kubelet-bootstrap": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "1h"
      }
    }
  }
}
