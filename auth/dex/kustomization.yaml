---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auth-dex

resources:
- ./dex.yaml

secretGenerator:
- name: dex-config
  files:
  - config.yaml=./env/dex-config.yaml
  # options:
  #   disableNameSuffixHash: true
