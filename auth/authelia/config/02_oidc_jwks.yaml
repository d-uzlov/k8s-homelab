---
# yaml-language-server: $schema=https://www.authelia.com/schemas/v4.39/json-schema/configuration.json

identity_providers:
  oidc:
    jwks:
    - algorithm: RS256
      use: sig
      key: |
{{- fileContent "/secrets/keys/jwks_key" | nindent 8 }}
