---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pv-dcsi

resources:
- deployment.gen.yaml
- storage-class.yaml

secretGenerator:
- name: driver-config
  files:
  - driver-config-file.yaml=env/config.yaml
