kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-claim
spec:
  storageClassName: nfs-64k-cached
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
