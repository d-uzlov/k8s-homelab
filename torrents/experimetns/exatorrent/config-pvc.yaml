---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: exatorrent-config
  namespace: torrents
spec:
  storageClassName: nfs-64k-cached
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
