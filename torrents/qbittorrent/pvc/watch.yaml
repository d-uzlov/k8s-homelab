---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: watch
spec:
  storageClassName: fast-persist
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
