---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: watch
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
