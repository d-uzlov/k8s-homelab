---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: watch
spec:
  storageClassName: ssd-persist
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
