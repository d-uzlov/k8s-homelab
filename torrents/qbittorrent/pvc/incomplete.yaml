---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: incomplete
spec:
  storageClassName: fast-persist
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Ti
