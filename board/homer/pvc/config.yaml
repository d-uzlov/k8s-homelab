---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
