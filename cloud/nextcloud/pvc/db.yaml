---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb-binlog
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
