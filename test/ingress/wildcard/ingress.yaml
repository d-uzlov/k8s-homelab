---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo-wildcard
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  ingressClassName: nginx
  tls:
  - secretName: REPLACE_ME
  rules:
  - host: echo-wildcard.REPLACE_ME.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: echo
            port:
              name: http
