---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: fortio-data
  namespace: fortio
spec:
  storageClassName: nfs-64k-cached
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
