---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ovenplayer
spec:
  ingressClassName: nginx
  tls:
  - secretName: REPLACE_ME
  rules:
  - host: player.REPLACE_ME.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ovenplayerdemo
            port:
              name: http
