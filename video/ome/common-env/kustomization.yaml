---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ome

configMapGenerator:
- name: webrtc-address
  envs:
  - ./env/webrtc-address.env

secretGenerator:
- name: passwords
  envs:
  - env/passwords.env
  options:
    labels:
      ome: passwords
      
