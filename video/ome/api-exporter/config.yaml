servers:
- name: main
  # apiUrl: http://api-origin:8081
  apiUrl: https://ome-api.meoe.cloudns.be
  apiAuth: Basic command!base64{{command!env{OME_AUTH_TOKEN}}}
  publicSignalUrl: signal.command!env{PUBLIC_SUFFIX}
debug: true
