---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: db
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
