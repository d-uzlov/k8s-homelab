---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: torrent
spec:
  storageClassName: AUTOMATIC_REPLACE
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: AUTOMATIC_REPLACE
