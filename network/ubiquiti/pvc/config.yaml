---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config
spec:
  storageClassName: fast-persist
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
